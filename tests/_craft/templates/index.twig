{# Placeholder index template — present so Craft's view component has a real
   templates directory to resolve against. Tests don't render front-end
   templates; this just keeps the resolver happy if a code path touches it. #}
