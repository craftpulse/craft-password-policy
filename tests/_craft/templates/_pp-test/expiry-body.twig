{# G11 fixture — site Twig template used by NotificationServiceTemplatePathTest
   to verify the Enterprise renderer-side branch in
   NotificationService::composeFromTemplate(). Producing a distinct,
   recognizable string lets the test assert "this output came from the
   file, not from the DB body". #}
[G11-FIXTURE] Body rendered from Twig file. daysUntilExpiry={{ daysUntilExpiry }} siteName={{ siteName }}
